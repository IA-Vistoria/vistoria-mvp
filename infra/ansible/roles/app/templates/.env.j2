# Gerado pelo Ansible — NUNCA editar à mão nem adicionar chave de API aqui.
# A VM se autentica via instance principal (ver docs/spec-backend.md).
OCI_AUTH_MODE=instance_principal
OCI_REGION={{ app_oci_region }}
OCI_COMPARTMENT_OCID={{ app_compartment_ocid }}
OCI_BUCKET_NAME={{ app_bucket_name }}
DB_URL={{ app_db_url }}
APP_IA_PROVIDER=oci
